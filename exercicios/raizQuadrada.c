#include <stdio.h>

int main() {
    
    float x, y;
    
    printf("Digite um numero para tirar raiz: ");
    scanf("%f", &x);
    
    for(y = 0; y < 10; y++){
        y = y - (y*y)
    }
    
    return 0;
}